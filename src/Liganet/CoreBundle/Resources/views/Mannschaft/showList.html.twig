<h2>Mannschaften
{% if isGrantedEdit == true %}
        <a style="float: right" class="btn" href="{{ path('mannschaft_new', { 'id': verein.id }) }}">
            <i class="icon-plus"></i>
        </a>
{% endif %}  
    </h2>
    <table class="table">
        <tbody>
        {% for mannschaft in entities %}
                {% include 'LiganetCoreBundle:Mannschaft:rowContent.html.twig' with {'mannschaft': mannschaft, 'isGrantedEdit': isGrantedEdit } %}
        {% endfor %}
            </tbody>
        </table>
