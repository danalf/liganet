{% extends '::base.html.twig' %}
{% block content %}

<div class="row-fluid">
    <div class="span11">
        <div class="page-header">
            <h1>Zuordnung Liga und Saison</h1>
        </div>
    </div>
    <div class="span1">
        {% if is_granted('ROLE_LEAGUE_MANAGEMENT') %}
        <a class="btn" href="{{ path('ligasaison_new') }}">
            <i class="icon-plus"></i>
        </a>
        {% endif %}    
    </div>
</div>

<table class="table table-condensed table-striped">
    <thead>
        <tr>
            <th>Saison</th>
            <th>Liga</th>
            <th>Gesperrt</th>
            <th>Bemerkung</th>
            <th>Aktionen</th>
        </tr>
    </thead>
    <tbody>
            {% for entity in entities %}
            <tr>
                <td>{{ entity.saison.saison }}</a></td>
                <td>{{ entity.liga.name }}</a></td>
                <td>
                    {% if entity.gesperrt > 0 %}
                    <i class="icon-ok"></i>
                    {% endif %}
                </td>
                
                <td>{{ entity.bemerkung }}</td>
                <td>
                        <a class="btn" href="{{ path('ligasaison_show', { 'id': entity.id }) }}">
                            <i class="icon-arrow-right"></i>
                        </a>
                        {% if isGrantedEdit == true %}
                        <a class="btn" href="{{ path('ligasaison_edit', { 'id': entity.id }) }}">
                            <i class="icon-pencil"></i>
                        </a>
                        {% endif %}
                    </td>
            </tr>
    {% endfor %}
        </tbody>
    </table>

{% endblock %}
