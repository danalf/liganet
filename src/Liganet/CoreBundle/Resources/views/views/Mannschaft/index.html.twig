{% extends '::base.html.twig' %}
{% block content %}

<div class="row-fluid">
    <div class="span11">
        <div class="page-header">
            <h1>Vereine</h1>
        </div>
    </div>
</div>

<table class="table">
    <thead>
        <tr>
            <th>Rang</th>
            <th>Bemerkung</th>
            <th>Kapitän</th>
            <th>Aktionen</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td>{{ entity }}</td>
            <td>{{ entity.bemerkung }}</td>
            <td>{{ entity.captain }}</td>
            <td>
                        <a class="btn" href="{{ path('mannschaft_show', { 'id': entity.id }) }}">
                            <i class="icon-arrow-right"></i>
                        </a>
                        {% if isGrantedEdit == true %}
                        <a class="btn" href="{{ path('mannschaft_edit', { 'id': entity.id }) }}">
                            <i class="icon-pencil"></i>
                        </a>
                        {% endif %}
                    </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

{% endblock %}