{% extends '::base.html.twig' %}
{% block content %}
<h1>Spieler creation</h1>

<form action="{{ path('spieler_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Erstellen</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('spieler') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
