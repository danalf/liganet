{% extends 'LiganetCoreBundle::base.html.twig' %}
{% block content %}
<div class="page-header">
    <h1>Verein hinzufügen</h1>
</div>

<form action="{{ path('verein_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Erstellen</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('verein') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
