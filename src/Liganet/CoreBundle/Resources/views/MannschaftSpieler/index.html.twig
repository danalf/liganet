{% extends '::base.html.twig' %}
{% block content %}

<div class="row">
    <div class="col-md-11">
        <div class="page-header">
            <h1>Mannschaftsspieler</h1>
        </div>
    </div>
</div>

<table class="table">
    <thead>
        <tr>
            <th>Bestaetigt</th>
            <th>Aktionen</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td>{{ entity.bestaetigt }}</td>
            <td>
                        <a class="btn btn-default" href="{{ path('mannschaftspieler_show', { 'id': entity.id }) }}">
                            <i class="fa fa-arrow-right"></i>
                        </a>
                        {% if isGrantedEdit == true %}
                        <a class="btn btn-default" href="{{ path('mannschaftspieler_edit', { 'id': entity.id }) }}">
                            <i class="fa fa-pencil"></i>
                        </a>
                        {% endif %}
                    </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

{% endblock %}
