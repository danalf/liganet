{% extends '::base.html.twig' %}
{% block content %}
<h1>MannschaftSpieler list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Bestaetigt</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('mannschaftspieler_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.bestaetigt }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('mannschaftspieler_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('mannschaftspieler_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('mannschaftspieler_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
{% endblock %}
