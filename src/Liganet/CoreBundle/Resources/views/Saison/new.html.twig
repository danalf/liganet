{% extends '::base.html.twig' %}
{% block content %}
<h1>Saison creation</h1>

<form action="{{ path('saison_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Erstellen</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('saison') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
