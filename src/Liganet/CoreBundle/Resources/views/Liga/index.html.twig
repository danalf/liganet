{% extends '::base.html.twig' %}
{% block content %}

<div class="row">
    <div class="col-md-11">
        <div class="page-header">
            <h1>Ligen</h1>
        </div>
    </div>
    <div class="col-md-1">
        {% if is_granted('ROLE_REGION_MANAGEMENT') %}
        <a class="btn btn-default" href="{{ path('liga_new') }}">
            <i class="fa fa-plus"></i>
        </a>
        {% endif %}    
    </div>
</div>


<table class="table">
    <thead>
        <tr>
            <th>Region</th>
            <th>Name</th>
            <th>Tabellenkopf</th>
            <th>Tabellenüberschrift</th>
            <th>Email</th>
            <th>Kuerzel</th>
            <th>Bemerkung</th>
            <th>Newsfeed</th>
            <th>Aktionen</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td>{{ entity.region }}</td>
            <td>{{ entity.name }}</td>
            <td style="background-color: {{ entity.farbeTabellenKopf }}; color: {{ entity.farbeTabellenKopfSchrift }};">Beispiel</td>
            <td style="background-color: {{ entity.farbeUeberschriftHintergrund }}; color: {{ entity.farbeUeberschrift }};">Beispiel</td>
            <td>{{ entity.email }}</td>
            <td>{{ entity.kuerzel }}</td>
            <td>{{ entity.bemerkung }}</td>
            <td>{{ entity.newsfeed }}</td>
            <td>
                        <a class="btn btn-default" href="{{ path('liga_show', { 'id': entity.id }) }}">
                            <i class="fa fa-arrow-right"></i>
                        </a>
                        {% if isGrantedEdit == true %}
                        <a class="btn btn-default" href="{{ path('liga_edit', { 'id': entity.id }) }}">
                            <i class="fa fa-pencil"></i>
                        </a>
                        {% endif %}
                    </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

{% endblock %}
