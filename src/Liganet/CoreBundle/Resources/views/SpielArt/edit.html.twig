{% extends '::base.html.twig' %}
{% block content %}
<h1>SpielArt edit</h1>

<form action="{{ path('spielart_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    {{ form_widget(edit_form) }}
    <p>
        <button class="btn btn-default" type="submit">Speichern</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('spielart') }}">
            Back to the list
        </a>
    </li>
    <li>
        <form action="{{ path('spielart_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}
