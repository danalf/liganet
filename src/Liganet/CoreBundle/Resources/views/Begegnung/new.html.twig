<h1>Begegnung creation</h1>

<form action="{{ path('begegnung_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Erstellen</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('begegnung') }}">
            Back to the list
        </a>
    </li>
</ul>
