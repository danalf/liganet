{% extends '::base.html.twig' %}
{% block content %}
<h1>SpielerStatus creation</h1>

<form action="{{ path('spielerstatus_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Erstellen</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('spielerstatus') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}