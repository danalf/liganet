{% extends '::base.html.twig' %}
{% block content %}
<div class="row">
    <div class="col-md-11">
        <div class="page-header">
            <h1>{{ entity.name }}</h1>
        </div>
    </div>
    <div class="col-md-1">
        <a class="btn btn-default" href="{{ path('spielerstatus') }}">
            <i class="fa fa-list"></i>
        </a>
        {% if isGrantedEdit == true %}
        <a class="btn btn-default" href="{{ path('spielerstatus_edit', { 'id': entity.id }) }}">
            <i class="fa fa-pencil"></i>
        </a>
        {% endif %}
        {% if is_granted('ROLE_ADMIN') %}
        <form  action="{{ path('spielerstatus_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button  class="btn btn-default"  type="submit"><i class="fa fa-trash"></i></button>
        </form>
        {% endif %}
    </div>
</div>

<table class="table">
    <tbody>
        <tr>
            <th>Name</th>
            <td>{{ entity.name }}</td>
        </tr>
        <tr>
            <th>Kurz</th>
            <td>{{ entity.kurz }}</td>
        </tr>
    </tbody>
</table>

{% endblock %}