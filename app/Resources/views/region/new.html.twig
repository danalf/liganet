{% extends 'base.html.twig' %}

{% block body %}
    <h1>Region erstellen</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Erstellen" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('region_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
