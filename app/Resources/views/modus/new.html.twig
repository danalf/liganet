{% extends 'base.html.twig' %}

{% block content %}
    <h1>Modus creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Erstellen" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('modus_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
