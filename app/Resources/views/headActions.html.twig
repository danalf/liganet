<div class="col-md-1">
    <a class="btn btn-default" href="{{ path(art ~ '_index') }}">
        <i class="fa fa-list"></i>
    </a>
    {% if isGrantedEdit == true %}
    <a class="btn btn-default" href="{{ path(art ~ '_edit', { 'id': id }) }}">
        <i class="fa fa-pencil"></i>
    </a>
    {% endif %}
    {% if is_granted('ROLE_ADMIN') %}
    <form  action="{{ path(art ~ '_delete', { 'id': id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button  class="btn btn-default"  type="submit"><i class="fa fa-trash"></i></button>
        </form>
        {% endif %}
    </div>
