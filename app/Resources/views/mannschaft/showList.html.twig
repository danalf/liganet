<h2>Mannschaften
    {% if is_granted("edit", verein) %}
        <a style="float: right" class="btn btn-default" href="{{ path('mannschaft_new', { 'id': verein.id }) }}">
            <i class="fa fa-plus"></i>
        </a>
    {% endif %}  
</h2>
<table class="table">
    <tbody>
        {% for mannschaft in verein.mannschaften %}
            {{ include('mannschaft/rowContent.html.twig', { 'mannschaft': mannschaft }) }}
        {% endfor %}
    </tbody>
</table>
