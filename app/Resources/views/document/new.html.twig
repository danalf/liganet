{% extends '::base.html.twig' %}
{% block content %}
<h1>Document creation</h1>
    {{ form_start(form, {'action': path('document_create'), 'method': 'POST'}) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('document') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
